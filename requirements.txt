# Core runtime dependencies for atomrdf.
# Mirrors [project.dependencies] in pyproject.toml — no strong pinning.
# For optional features (autoquery, oxigraph, …) see
# [project.optional-dependencies], e.g. pip install "atomrdf[all]".
numpy
pandas
ase
pyscal3
spglib
mendeleev
rdflib
pydantic
pyyaml
graphviz
