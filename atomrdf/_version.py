"""Single source of truth for the atomrdf package version.

Bump this for releases. ``pyproject.toml`` and ``atomrdf.__version__`` both read
from here so the version only needs to change in one place.
"""

__version__ = "1.0.0"
